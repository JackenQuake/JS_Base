'use strict';

let a = 2;              // a = 2;
let x = 1 + (a *= 2);   // Сперва a *= 2 ~ a = a * 2 = 2 * 2 = 4; Затем: x = 1 + (значение скобки - новое a) = 1 + 4 = 5;

alert(a);               // Вывод: 4
alert(x);               // Вывод: 5